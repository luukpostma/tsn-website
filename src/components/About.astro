---
import Button from './Button.astro';
---

<section class="about">
  <div class="container">
    <div class="about__header">
      <div class="about__intro">
        <p class="about__label">
          <img src="/icons/line.svg" alt="" />
          Over ons
        </p>
        <h3>Timmerfabriek Sint Nyk,<br /><span>hout met oog voor detail.</span></h3>
        <p class="about__text">
          Timmerfabriek Sint Nyk is een lokaal bedrijf met 20+ jaar ervaring in houten timmerwerk. Ons jong, ambitieus team levert houten kozijnen, ramen en deuren met aandacht voor detail en vakmanschap. We focussen op langdurige klantrelaties en projecten die blijven staan.
        </p>
      </div>
      <div class="about__cta">
        <Button href="/over-ons">Over ons</Button>
      </div>
    </div>

    <div class="about__image">
      <img src="/images/over-ons-image.webp" alt="Timmerfabriek Sint Nyk aan het werk" />
    </div>
  </div>
</section>

<style>
  .about {
    background-color: var(--gray-200);
  }

  .about__header {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-32);
    margin-bottom: var(--spacing-48);
  }

  .about__label {
    display: flex;
    align-items: center;
    gap: var(--spacing-12);
    font-weight: var(--font-semibold);
    margin-bottom: var(--spacing-16);
  }

  .about__label img {
    width: 24px;
  }

  .about__intro h3 {
    margin-bottom: var(--spacing-24);
  }

  .about__intro h3 span {
    font-weight: var(--font-regular);
  }

  .about__text {
    max-width: 768px;
  }

  .about__cta {
    display: flex;
  }

  .about__cta :global(.button) {
    width: 100%;
    justify-content: center;
  }

  .about__image img {
    width: 100%;
    height: 240px;
    object-fit: cover;
  }

  /* Desktop */
  @media (min-width: 1024px) {
    .about__header {
      flex-direction: row;
      justify-content: space-between;
      align-items: flex-end;
      margin-bottom: var(--spacing-64);
    }

    .about__cta :global(.button) {
      width: auto;
    }

    .about__image img {
      height: 480px;
    }
  }
</style>
