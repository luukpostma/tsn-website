 ---
import { getCollection, render, type CollectionEntry } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import Button from '../../components/Button.astro';

export async function getStaticPaths() {
  const projects = await getCollection('projects');
  return projects.map((project: { id: string; }) => ({
    params: { slug: project.id.replace('.md', '') },
    props: { project },
  }));
}

interface Props {
  project: CollectionEntry<'projects'>;
}

const { project } = Astro.props;
const { Content } = await render(project);
---

<Layout title={project.data.title}>
  <section class="project-hero">
    <div class="container">
      <h1>{project.data.title}</h1>
      <p class="project-hero__description">{project.data.description}</p>
    </div>
  </section>

  <div class="project-image">
    <div class="container">
      <img src={project.data.image} alt={project.data.title} />
    </div>
  </div>

  <article class="project-content">
    <div class="container">
      <div class="project-content__grid">
        <div class="project-details">
          <h5>Details</h5>
          <ul class="details-list">
            <li>
              <span class="details-label">Locatie:</span> {project.data.location}
            </li>
            <li>
              <span class="details-label">Opdrachtgever:</span> {project.data.client}
            </li>
            <li>
              <span class="details-label">Service:</span> {project.data.category}
            </li>
            <li>
              <span class="details-label">Datum:</span> {project.data.date}
            </li>
          </ul>
        </div>
        <div class="project-story">
          <h5>Project verhaal</h5>
          <Content />
        </div>
      </div>
    </div>
  </article>

  <section class="project-cta">
    <div class="container">
      <div class="project-cta__content">
        <div class="project-cta__text">
          <span class="project-cta__label">— Contact</span>
          <h2>Klaar voor uw kozijnen?<br /><span>Laten we starten.</span></h2>
        </div>
        <Button href="/contact" variant="dark-blue">Contact opnemen</Button>
      </div>
    </div>
  </section>
</Layout>

<style>
  .project-hero {
    background-color: var(--dark-blue-500);
  }

  .project-hero h1 {
    color: var(--white);
    margin-bottom: var(--spacing-24);
  }

  .project-hero__description {
    color: var(--white);
    opacity: 0.9;
    max-width: 768px;
  }

  .project-image {
    padding: var(--spacing-48) 0;
  }

  .project-image .container {
    padding-top: 0;
    padding-bottom: 0;
  }

  .project-image img {
    width: 100%;
    height: 300px;
    object-fit: cover;
  }

  .project-content .container {
    padding-top: 0;
  }

  .project-content__grid {
    display: grid;
    gap: var(--spacing-48);
  }

  .project-details h5,
  .project-story h5 {
    margin-bottom: var(--spacing-24);
  }

  .details-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .details-list li {
    padding: var(--spacing-16) 0;
    border-bottom: 1px solid var(--gray-300);
  }

  .details-list li:first-child {
    padding-top: 0;
  }

  .project-story :global(p) {
    margin-bottom: var(--spacing-24);
  }

  .project-story :global(p:last-child) {
    margin-bottom: 0;
  }

  /* CTA Section */
  .project-cta {
    background-color: var(--green-500);
  }

  .project-cta__content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--spacing-32);
  }

  .project-cta__label {
    color: var(--gray-900);
    font-weight: var(--font-semibold);
    margin-bottom: var(--spacing-8);
    display: block;
  }

  .project-cta h2 {
    color: var(--gray-900);
    font-weight: var(--font-bold);
    margin: 0;
  }

  .project-cta h2 span {
    color: var(--gray-900);
    font-weight: var(--font-regular);
  }

  /* Desktop */
  @media (min-width: 1024px) {
    .project-image img {
      height: 560px;
    }

    .project-content__grid {
      grid-template-columns: 1fr 2fr;
      gap: var(--spacing-64);
    }

    .project-cta__content {
      flex-direction: row;
      justify-content: space-between;
      align-items: flex-end;
    }
  }
</style>
